So the md file is to explain how the login system works
# Tokens and Middleware
This middleware handles authentication tokens for API requests from 'JWT.ts'. It verifies the presence and validity of tokens in incoming requests to secure protected routes.